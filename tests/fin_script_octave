
% Vecteur colonne des normes des deltatheta
normes=sum(abs(A'))';

% Vecteur colonne du nombre d'itérations
N=length(normes);
iterations=[1:N]';

% Tracé
plot(iterations(50:end), normes(50:end), '*-');
hold on;
title('Evolution des increments au cours des iterations');
hold off;
